# Recruiter AI Coach 🎯

AI-ассистент для рекрутеров в реальном времени. Слушает звонок через Fireflies.ai, генерирует умные подсказки и вопросы прямо во время собеседования.

## Что умеет

### Во время звонка
- Получает live-транскрипцию от Fireflies.ai через WebSocket
- Claude AI анализирует разговор и генерирует подсказки рекрутеру
- Подсказки: уточняющие вопросы, красные флаги, технические детали, следующие шаги
- Доставка: Telegram-бот (MVP) → Chrome Extension (prod)

### До звонка — подготовка рекрутера
- Рекрутер загружает резюме кандидата + описание вакансии
- AI генерирует: ключевые термины отрасли, примеры вопросов типа "ты используешь фреймворк A или B?", глоссарий, на что обратить внимание
- Рекрутер тратит 5 минут и идёт на звонок подготовленным

## Архитектура

```
Fireflies Bot (в звонке)
    ↓ WebSocket (Realtime API)
Backend (Node.js)
    ↓ Claude API → генерация подсказок
    ↓ WebSocket
Telegram Bot (MVP) | Chrome Extension (prod)
    ↓
Рекрутер видит подсказки
```

## Стек
- **Backend**: Node.js, Express, Socket.IO
- **AI**: Claude API (claude-sonnet-4-6)
- **Транскрипция**: Fireflies.ai Realtime API
- **MVP доставка**: Telegram Bot (node-telegram-bot-api)
- **Prod доставка**: Chrome Extension

## Быстрый старт

### 1. Установка
```bash
cd backend
npm install
```

### 2. Переменные окружения
```bash
cp ../.env.example .env
# Заполни FIREFLIES_API_KEY, ANTHROPIC_API_KEY, TELEGRAM_BOT_TOKEN
```

### 3. Запуск
```bash
npm start
```

### 4. Подготовка к звонку
```bash
# Загрузить кандидата и получить prep materials
curl -X POST http://localhost:3000/api/prepare \
  -H "Content-Type: application/json" \
  -d '{"candidateCV": "...", "jobDescription": "...", "role": "Frontend Engineer"}'
```

### 5. Начать сессию мониторинга
```bash
# Передаёт transcriptId активного митинга из Fireflies
curl -X POST http://localhost:3000/api/session/start \
  -d '{"transcriptId": "xxx"}'
```

## Переменные окружения

| Переменная | Описание |
|-----------|---------|
| `FIREFLIES_API_KEY` | API ключ от Fireflies.ai (Settings → Developer) |
| `ANTHROPIC_API_KEY` | Claude API ключ |
| `TELEGRAM_BOT_TOKEN` | Токен бота от @BotFather |
| `TELEGRAM_CHAT_ID` | ID чата/пользователя для отправки подсказок |
| `PORT` | Порт сервера (default: 3000) |

## Структура проекта

```
recruiter-ai-coach/
├── backend/
│   ├── src/
│   │   ├── server.js         # Express + WebSocket сервер
│   │   ├── fireflies.js      # Fireflies Realtime API клиент
│   │   ├── claude.js         # Генерация подсказок через Claude
│   │   ├── telegram.js       # Telegram бот доставка
│   │   ├── session.js        # Управление сессиями
│   │   └── prep.js           # Pre-interview подготовка
│   └── package.json
├── chrome-extension/
│   ├── manifest.json
│   ├── popup.html
│   ├── popup.js
│   ├── content.js            # Overlay на Google Meet / Zoom
│   └── background.js
├── TASKS.md                  # Roadmap и статус задач
└── .env.example
```

## Как тестировать

1. Войди в Fireflies.ai, убедись что бот добавлен в Google Calendar
2. Начни Google Meet звонок — Fireflies бот подключится автоматически
3. Запусти `npm start`
4. POST на `/api/session/start` с transcriptId из Fireflies dashboard
5. Говори что-нибудь — подсказки придут в Telegram

## Roadmap

Подробнее в [TASKS.md](TASKS.md)

- [x] Архитектура и документация
- [ ] Fireflies Realtime WebSocket интеграция
- [ ] Claude hint generation
- [ ] Telegram bot доставка
- [ ] Pre-interview prep endpoint
- [ ] Chrome Extension MVP
- [ ] Тестирование с реальным звонком
